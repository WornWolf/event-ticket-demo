<% layout('layout') -%>
<div
  class="d-flex justify-content-center align-items-center"
  style="min-height: 50vh"
>
  <div class="card shadow-soft text-center" style="max-width: 560px">
    <div class="card-body p-5">
      <div class="display-6 mb-2">🚫</div>
      <h2 class="h3 mb-2">403 Forbidden</h2>
      <p class="text-muted mb-4">
        คุณไม่มีสิทธิ์เข้าถึงหน้านี้ <% if (!currentUser) {
        %>กรุณาเข้าสู่ระบบก่อนจึงจะใช้งานต่อได้<% } %>
      </p>
      <div class="d-flex gap-2 justify-content-center">
        <% if (!currentUser) { %>
        <a href="/login" class="btn btn-primary"
          ><i class="bi bi-box-arrow-in-right"></i> เข้าสู่ระบบ</a
        >
        <a href="/" class="btn btn-outline-secondary"
          ><i class="bi bi-house"></i> หน้าแรก</a
        >
        <% } else { %>
        <a href="/" class="btn btn-primary"
          ><i class="bi bi-house"></i> หน้าแรก</a
        >
        <button class="btn btn-outline-secondary" onclick="history.back()">
          <i class="bi bi-arrow-left"></i> ย้อนกลับ
        </button>
        <% } %>
      </div>
    </div>
  </div>
</div>
