<% layout('layout') -%>

<section class="text-center py-5 bg-light rounded shadow-sm mb-4" id="search-box">
  <h1 class="fw-bold mb-2" id="search-events">📅 ค้นหาอีเวนต์</h1>
  <p class="text-muted">ดูอีเวนต์ล่าสุด กิจกรรม และงานที่คุณสนใจได้ที่นี่</p>

  <form class="row justify-content-center mt-4" method="get" action="/">
    <div class="col-md-6 col-sm-8 mb-2">
      <input
        type="text"
        name="q"
        class="form-control form-control-lg"
        placeholder="🔍 พิมพ์ชื่ออีเวนต์ที่ต้องการค้นหา..."
        value="<%= q %>"
      />
    </div>

    <div class="col-auto">
      <button class="btn btn-primary btn-lg px-4" id="search-btn">ค้นหา</button>
    </div>
  </form>
</section>

<div class="row g-4">
  <% if (!events || !events.length) { %>
    <div class="text-center text-muted py-5">
      <i class="bi bi-calendar-x display-6 d-block mb-2"></i>
      ยังไม่มีอีเวนต์ในขณะนี้
    </div>
  <% } else { %>
    <% events.forEach(ev => { %>
      <%- include('partials/event-card', { ev }) %>
    <% }) %>
  <% } %>
</div>

<link rel="stylesheet" href="css/index.css">


